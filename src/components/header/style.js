import styled from "styled-components";
import tw from "twin.macro";

export const HeaderContainer = styled.div`
  box-shadow: 0px 3px 5px -1px rgba(66, 68, 90, 1);
  
  ${tw`
  w-full
  h-16
  flex 
  justify-center
  items-center
  `}
`