import styled from "styled-components";
import tw from "twin.macro";

export const FormContainer = styled.div`
  ${tw`
    w-screen
    mt-20
    mx-auto
    bg-green-50
    flex
    justify-center 
  `}
`

